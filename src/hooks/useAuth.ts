// This file can be deleted since we're now using the AuthContext
// But if you want to keep it for compatibility, you can re-export:

export { useAuth } from '../contexts/AuthContext';
export type { AuthUser } from '../services/cognitoAuth';